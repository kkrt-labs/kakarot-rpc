---
name: Format indexer

on:
  workflow_call:

jobs:
  spell-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: indexer
      - name: Run formatting
        run: deno fmt --check indexer/
